import React from "react";

const SectionTitle = ({ title }) => {
  return (
    <div>
      <h3 className="text-2xl text-cod-gray font-semibold mt-4">{title}</h3>
    </div>
  );
};

export default SectionTitle;
